## Copy other tests setup structure to here

chai = require('chai');
should = chai.should();

describe "Transfer ether from one account to another", ->
  it 'should get the balances before the the transfer for first 2 accounts', ->
    console.log 'not used yet'
   # Contract.account1.should.be.an('Object')

  it 'should get the balances before the the transfer for first 2 accounts', ->
    console.log 'not yet implemented, only single account model currently'

 #   Contract.account2.should.be.an('Object')
